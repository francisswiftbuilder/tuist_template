import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.makeModule(
    name: ModulePathEnvironment.{{ layer }}.name+ModulePathEnvironment.{{ layer }}.{{ name }}.rawValue,
    targets: {{ target }}
)
